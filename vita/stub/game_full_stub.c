/* Vita Phase 0 stub for generated/slus_006.64_full_*.c.
 * The recompiled game code is dispatched only through the game dispatch
 * table, so the runtime references no symbol from these shards directly;
 * this TU exists so the link shape matches the real full build. */
#include "cpu_state.h"
